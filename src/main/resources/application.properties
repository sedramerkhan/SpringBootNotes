spring.application.name=Spring Boot Notes

#server.port=8085
spring.data.mongodb.uri=${MONGODB_CONNECTION_STRING}
spring.mongodb.uri=${MONGODB_CONNECTION_STRING}
#spring.security.user.password = test # no need for this any more with our security
jwt.secret=${JWT_SECRET_BASE64}
spring.data.mongodb.auto-index-creation=true